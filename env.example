# Database connection (use port 5433 to avoid conflicts with other PostgreSQL instances)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/faceland?schema=public"

# Admin Authentication
# Set a secure password for the admin panel
ADMIN_PASSWORD="admin123"

# Session Secret
# Generate a secure secret with: openssl rand -base64 32
SESSION_SECRET="change-this-to-a-secure-secret-key"

# App URL (used for webhook callbacks and HTML export)
# Change this to your production URL when deploying
NEXT_PUBLIC_APP_URL="http://localhost:3000"

